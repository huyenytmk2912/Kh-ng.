import express from 'express';import{spawn}from'node:child_process';const app=express();app.use(express.json());app.use(express.static('web'));let p=null;app.post('/api/start',(q,r)=>{if(p)return r.json({message:'Agent đang chạy'});let{message,count=1,delay=10}=q.body||{};if(!message)return r.status(400).json({message:'Thiếu tin nhắn'});p=spawn('node',['agent/chatgpt-agent.js'],{env:{...process.env,MESSAGE:message,COUNT:String(count),DELAY:String(delay)},stdio:'inherit'});p.on('close',()=>p=null);r.json({message:'Đã khởi động'});});app.post('/api/stop',(q,r)=>{if(p){p.kill('SIGTERM');p=null}r.json({message:'Đã dừng'})});app.listen(process.env.PORT||3000,'0.0.0.0',()=>console.log('Dashboard :3000'));